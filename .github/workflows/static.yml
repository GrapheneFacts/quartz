# Disabled on purpose.
# GitHub's "Deploy static content" starter uploaded the raw repository
# (no index.html), which 404'd at graphenefacts.github.io/quartz.
# The real site is built and deployed by deploy.yml.
name: Deploy static content to Pages (disabled)

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy.yml instead."
